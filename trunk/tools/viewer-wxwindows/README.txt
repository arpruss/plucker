A simple Plucker viewer for desktop's using a wxWindows GUI

Bill Nalen (bill@nalens.com)
Copyright 2003
Licensed under the GPL license

Uses the explode library /tools/explode to convert the Plucker. PDB
into HTML and images, and then reads the HTML inside a HTML window.

Currently requires a diff for explode (included)